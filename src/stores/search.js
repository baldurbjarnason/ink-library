
import { writable } from 'svelte/store';

export const searchResults = writable()
export const isEmpty = writable();
export const sourcesEmpty = writable();
export const notesEmpty = writable();
export const notebooksEmpty = writable();

export const search = writable()
